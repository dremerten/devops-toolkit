apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: devops-toolkit-staging-cert
  namespace: nginx-gateway
spec:
  secretName: devops-toolkit-staging-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-gateway
  dnsNames:
  - staging-devops-toolkit.dremer10.com
